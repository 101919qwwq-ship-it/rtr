{
  "name": "droid-crud-railway",
  "version": "1.0.0",
  "description": "Droid CRUD app (Express + Postgres) ready for Railway",
  "main": "server.js",
  "scripts": {
    "start": "node migrate.js && node server.js",
    "migrate": "node migrate.js"
  },
  "engines": {
    "node": ">=16"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.2",
    "express-validator": "^6.14.3",
    "pg": "^8.10.0"
  }
}